/*
SHA-512 算法主要步骤：

1. 填充消息
   - **目的**：确保消息长度满足“模 1024 = 896”的条件（即最后留出 128 位用于表示原始消息长度）。
   - **填充方式**：
     1. 在消息末尾添加一个“1”位。
     2. 添加若干个“0”位，直到消息长度满足上述条件。
     3. 最后附加原始消息长度（以 128 位整数表示）。
   - **原因**：
     - 附加原始消息长度是为了确保消息的完整性。
     - 填充“1”和“0”是为了满足算法对消息长度的要求。

2. 分块处理
   - **操作**：将填充后的消息分成多个 1024 位（128 字节）大小的块，每块独立处理。

3. 初始哈希值（IV）
   - **定义**：使用 8 个 64 位的初始值。
   - **来源**：这些值是质数平方根的小数部分。

4. 轮常量（K[0..79]）
   - **定义**：80 个 64 位的常量。
   - **来源**：这些常量通常取自质数立方根的小数部分。

5. 消息调度数组（W）
   - **作用**：在每轮压缩函数中，生成和使用消息调度数组。

6. 压缩函数与迭代更新
   - **操作**：对每个消息块，进行 80 轮压缩函数的迭代运算。
   - **特点**：
     - 使用位运算符 `& 0xFFFFFFFFFFFFFFFF` 对每次计算结果进行模运算，保留结果的低 64 位。
     - **自动溢出处理**：在 64 位架构下，编程语言会自动处理整数溢出，相当于对 `2^64` 取模。
*/

